@model IEnumerable<DV_MvcApp.Models.DV_Author>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DV_FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DV_LastName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DV_FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DV_LastName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DV_AuthorId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DV_AuthorId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DV_AuthorId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
