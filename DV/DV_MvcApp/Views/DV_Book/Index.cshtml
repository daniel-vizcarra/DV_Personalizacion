@model IEnumerable<DV_MvcApp.Models.DV_Book>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DV_Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DV_Author)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DV_Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DV_Author.DV_AuthorId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DV_BookId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DV_BookId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DV_BookId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
